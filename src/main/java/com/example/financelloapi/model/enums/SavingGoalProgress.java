package com.example.financelloapi.model.enums;

public enum SavingGoalProgress {
    CREATED,       // cuando el usuario recién crea la meta
    IN_PROGRESS,   // cuando empieza a contribuir
    DONE      // cuando llega al monto objetivo
}